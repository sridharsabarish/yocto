## Task

* Create a makefile called "Makefie with two targets `build` and `clean`
    * An example starter is given below!


```make


HW?=""  #something comes here
IMAGE_NAME?=capstone #change to your image name

#target1 what should it be called?
#   MACHINE=${HW} bitbake  ${IMAGE_NAME}

 # target2? what should it be called? 
 #  MACHINE=${HW} bitbake  ${IMAGE_NAME} -c [nameofthetask] #which task can we invoke to clean a recipe?




```