# Exercise: Create a recipe
## Learning Outcome

* Learn different ways of creating a recipe.
   * Manual Method
   * Recipes tool
   * Devtool (Advanced SDK) 

* Learn how to add a task to a recipe that performs a certain action
* Learn how to create a bitbake class
* Learn the difference between an `inc` and `bbclass`


**NOTE :** Refer to slides for more instruction on how to create a recipe, this exercise will have limited hints/answers"

## Part 1 : Creating a recipe
### 1. Create a recipe `manually` 

1. Go to the folder `meta-exercises`.
2. Create a folder called`recipes-createrecipes`, ignore the pun.
3. Several example recipe are available at `~/Desktop/Yocto_Course/Examples/example.bb`, copy that recipe to this folder, Choose one and copy it to this folder.
   

```markdown
Irrespective of their size, the core elements you need in a recipe is 
`SUMMARY`, `DESCRIPTION` and `LICENSE`. 
```

4. Create your own recipe file and name this file as `manualrecipe.bb`. 
Try setting `SUMMARY` and `DESCRIPTION` to a different value to make your recipe different.

Remember the recipe should have its own folder, so it should be `/recipes-createrecipes/manualrecipe/manualrecipe.bb`.
5. Try to run this new recipe using the command `bitbake manualrecipe`, your simple recipe is now ready!!


### 2. Creating a recipe using `recipestools`

6. Now lets shift gears and try to make a recipe using recipestools.
   
   We can create a new recipe with recipestool by using the syntax below.
   
Syntax : `recipetool create -o OUTFILE source`
 
 Create a new folder called `inventory` using `mkdir inventory`

* go to the inventory folder using `cd inventory`

* In the syntax above replace OUTFILE with `inventory.bb` and add the source as `https://github.com/sridharsabarish/easyInventory.git`.
  
 This would create a simple recipe, with information of the source populated.

   <details>
   <summary>Solution</summary>
   - `recipetool create -o inventory.bb https://github.com/sridharsabarish/easyInventory.git`
   </details>

* run bitbake with your new recipe!

### 3. Create a recipe using `devtool`
  
   Okay, that was easy, how about using the yocto's devtool? 
   
   7. Create a recipe using devtool using the steps below

* Create a directory called devtoolrecipe
* Run the following command 
* `devtool add <Recipename> https://github.com/sridharsabarish/automate.git --srcbranch main`
* This would create a recipe that can be found in the `~/git/poky/build/workspace/recipes/` directory, move this recipe to the folder created earlier.

   <details>
   <summary>Solution</summary>
   - `devtool add devtoolrecipe .`cd ..
   </details>

* Run `bitbake {yourrecipename}`

## Enriching the manually created recipe

8. Add some metadata to the recipe `manualrecipe.bb`,  You can use your own data or you can add the following.
   

```shell
PV = "1.0+git${SRCPV}"
PR = "r1"

# Custom metadata
RING_INSCRIPTION = "One ring to rule them all, one ring to find them, one ring to bring them all, and in the darkness bind them"
MIDDLE_EARTH_REGION = "The Shire"
ENEMY_NAME = "Sauron"
ENEMY_DOMAIN = "Mordor"


```

###  Adding Task to your recipe


Now, we will add some tasks to our recipe. The tasks structure is provided for you, can you add them to your recipe. 

**Remember you need to explicitly add your task!**


#### General Syntax of a task

```pythonpython
do_nameoftask( ){

--- some content----
}

addtask nameoftask after <someothertask>  
// Note: You could replace <somothertask> with install, compile, build etc.
```

9. Go to the `manualrecipe.bb` and add two tasks `do_display_banner()` and `do_print_message()`

as given below


```
do_display_banner() {
    bbplain "*********************************************************************************************"
    bbplain "*                                                                                           *"
    bbplain "*  Example recipe created by Golem                                                *"
    bbplain "*                                                                                           *"
    bbplain "*  Version: ${PV}                                                                           *"
    bbplain "*  Revision: ${PR}                                                                          *"
    bbplain "*  License: ${LICENSE}                                                                      *"
    bbplain "*  ${RING_INSCRIPTION}                                                                      *"
    bbplain "*  ${MIDDLE_EARTH_REGION}                                                                   *"
    bbplain "*  Enemy: ${ENEMY_NAME} of ${ENEMY_DOMAIN}                                                  *"
    bbplain "*                                                                                           *"
    bbplain "*********************************************************************************************"
}

addtask display_banner before do_compile


do_print_message() {
    bbplain "This is a message from do_print_message()"
    bbplain "${RING_INSCRIPTION}"
    bbplain "${MIDDLE_EARTH_REGION}"
    bbplain "Beware of ${ENEMY_NAME} from ${ENEMY_DOMAIN}"
}

addtask print_message before do_compile

```


* Add one extra print statement. You can choose from `bbwarn`, `bberror`.
  
* The syntax is usually `bbtype "message"`, where bbtype is `bbwarn`, `bberror`, `bbnote`, `bbfatal` and so on.
  
* **Note:** Remember you need to **explicitly add the tasks** using addtask command, so add the two tasks before build.

## Part 3: Create a class

Go to the `~/git/poky/meta-exercises` and create a directory called `mkdir -p classes`

10. Create a new file called `lor.bbclass` and put it under the classes directory
11. Move the two tasks created to this file. Nake sure that you include the statements used to add the tasks.


## Part 3: Inherit this class in the created recipe 

Note: it is advisable to clean sstate for this recipe so that the tasks can be rebuilt. You can do this with
`bitbake -c cleansstate manualrecipe`

12. Inherit the class in our `manualrecipe.bb` this should bring the functions back to the class and try to print them.

   <details>
   <summary>Answer</summary>
   - add `inherit lor` to you manualrecipe.bb file
   </details>

## Part 5: Create incl files 

 14.  Create a folder called `include` under the layer's conf directory
 15. Now create a file called `lor.inc`
 - Now move the content of the metadata using the `include` directive in the recipe.
   <details>
   <summary>Answer</summary>
   - add `require conf/include/lor.inc` to you manualrecipe.bb file
   </details>

This way by using includes and classes, we can organize our codes and improve readability  and reusability.



## Part 6 : Extra Task (Optional/Advanced)
* Try to use recipetool to create a recipe `.bb` file from a different repo in github . Note: don't build it with bitbake to avoid delays.
* For this you can take a look into some inspirations in the bookmarks!